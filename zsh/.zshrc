# alias
alias l='ls -lh'
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

alias glf='git log --pretty=format:"%C(yellow) %h %C(bold #de6f66) %d %Creset  %s %C(green) %an <%cd>" --date=format:"%Y-%m-%d %H:%M" --graph --all'

#alias pbcopy='xclip -i -selection clipboar'
#alias pbpaste='xclip -o -selection clipboar'
#alias open="xdg-open"

# python tool
# https://github.com/OrkoHunter/keep
alias kun='keep run'


# set default editor to vim
export VISUAL=vim
export EDITOR="$VISUAL"
